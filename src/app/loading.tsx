import { LoadingSpiner } from "@/components/shared/LoadingUISkeletons/LoadingSpiner";

const Loading = () => {
  return (
    <div>
      <LoadingSpiner />
    </div>
  );
};

export default Loading;
